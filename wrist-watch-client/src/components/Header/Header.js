import "./Header.scss";
import { Link } from "react-router-dom";

function Header() {
	return (
		<>
			<header className="Header">
				<nav className="Nav"></nav>
			</header>
		</>
	);
}

export default Header;
