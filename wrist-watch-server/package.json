{
	"name": "wrist-watch-server",
	"version": "1.0.0",
	"description": "A server that contains all the video data",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node --watch server.js",
		"migrate": "knex migrate:latest",
		"seed": "knex seed:run"
	},
	"author": "Andy Jemedafe",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.3.1",
		"express": "^4.18.2",
		"express-router": "^0.0.1",
		"knex": "^3.0.1",
		"mysql2": "^3.6.3"
	}
}
